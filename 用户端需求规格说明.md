# 研学导览用户端需求规格说明

> 文档版本：1.0  
> 编制日期：2026-07-10  
> 适用项目：Japp Android 客户端  
> 依据：当前 `app` 模块、网络接口模型、页面布局及项目状态定义

## 1. 文档目的

本文档面向产品、设计、Android、后端及测试人员，在现有项目基础上定义一套可开发、可验收的用户端需求。文档既记录当前已经具备的能力，也标明需要完善的交互和业务闭环。

用户端的核心目标是：让普通用户可以低门槛生成研学路线、发起或加入拼单、匹配领队，并在出行前后完成沟通和行程查看。

用户与领队围绕拼单项目的状态变化见根目录文档：[用户与领队交互状态图.md](./用户与领队交互状态图.md)。

## 2. 产品范围

### 2.1 目标用户

- 研学参与者：学生家长、学生、教师或其他希望参加研学活动的普通用户。
- 拼单发起人：创建路线并发布项目的用户，同时是项目所有者。
- 拼单参与者：浏览并加入其他用户项目的普通用户。
- 领队：浏览可接项目、接单并负责线下带队的服务方。

### 2.2 用户端信息架构

用户登录后进入四个一级页面：

1. 路线：对话式 AI 路线规划、地图预览和发布拼单。
2. 拼单：浏览、筛选、查看和加入研学项目。
3. 消息：查看单聊/群聊会话及未读消息。
4. 我的：个人资料、历史路线和设置。

### 2.3 不在当前版本范围内

- 在线支付、退款、结算和发票。
- 领队资质审核后台。
- 保险购买、电子合同和紧急救援调度。
- Web 管理后台。
- 复杂社交关系、动态广场或公开评论系统。

上述能力可作为后续版本规划，但不能作为当前主链路上线的隐含依赖。

## 3. 需求标记

### 3.1 优先级

- P0：核心闭环，没有该能力则产品无法正常使用或存在严重安全问题。
- P1：重要体验，建议首个可用版本具备。
- P2：增强能力，可在核心链路稳定后迭代。

### 3.2 实现状态

- 已实现：当前代码已有完整或基本可用的页面与调用链。
- 部分实现：已有界面或局部逻辑，但仍依赖演示数据、缺少后端接口或缺少异常闭环。
- 待实现：当前代码没有形成可用能力。

## 4. 核心用户旅程

### 4.1 新用户首次使用

1. 用户打开应用，查看启动页并进入登录入口。
2. 用户选择注册，填写手机号、密码、验证码和基础资料。
3. 用户选择“普通用户”“领队”或“双角色”。
4. 注册成功后自动登录；普通用户进入用户端首页。
5. 用户可继续补充头像、昵称、地区、简介和研学偏好。

### 4.2 生成路线并发起拼单

1. 用户在“路线”页输入城市、天数、同行人群和研学主题。
2. 系统展示生成进度并调用 AI 路线接口。
3. 系统展示按访问顺序排列的路线节点、预计时长和地图线路。
4. 用户全屏查看地图，确认路线后进入发布设置。
5. 用户设置标题、出发日期、人数上限、地区和主题标签。
6. 系统创建 `OPEN` 状态的拼单并跳转或引导用户到拼单详情。

### 4.3 浏览并加入拼单

1. 用户进入“拼单”页，查看按业务状态排序的项目列表。
2. 用户按关键词、地区、主题、状态、日期、是否有领队和是否可加入进行筛选。
3. 用户打开详情，查看发起人、日期、人数、路线节点和地图。
4. 项目仍可加入且未满员时，用户确认加入。
5. 加入成功后人数刷新；已有领队时创建或复用聊天会话。

### 4.4 与领队沟通并参加活动

1. 领队接单后，项目参与者收到状态和消息提醒。
2. 用户通过项目会话确认集合点、时间、装备和注意事项。
3. 项目确认后不再允许普通用户直接加入。
4. 活动开始后项目进入“进行中”，用户可查看路线和领队信息。
5. 活动结束后项目进入“已完成”，用户可在历史路线中查看记录。

## 5. 功能需求

## 5.1 启动、注册与登录

### UR-AUTH-01 启动路由

- 优先级：P0
- 当前状态：已实现
- 需求：启动页完成初始化后，应根据登录凭证、自动登录开关和上次使用角色进入对应首页；凭证缺失或失效时进入登录入口。
- 验收标准：
  - 未登录用户不会直接进入业务首页。
  - 开启自动登录且 Token、账号 ID 有效时，可进入上次使用的用户端或领队端。
  - 收到 HTTP 401 后清理 Token 与账号状态，并返回登录页且清空返回栈。

### UR-AUTH-02 手机号密码登录

- 优先级：P0
- 当前状态：已实现
- 需求：用户输入 11 位手机号和非空密码后调用登录接口；成功后持久化 Token、账号 ID、角色范围和必要的展示信息。
- 验收标准：
  - 手机号格式错误、手机号为空或密码为空时，不发起网络请求并显示字段级错误。
  - 登录按钮请求期间不可重复点击。
  - 服务端错误优先展示业务消息，网络错误提供可重试提示。
  - 登录成功后不能通过返回键回到登录表单。

### UR-AUTH-03 验证码登录

- 优先级：P1
- 当前状态：部分实现
- 需求：验证码必须由服务端或短信平台发送、校验并设置有效期、频率限制和错误次数限制，不得使用客户端固定验证码。
- 验收标准：
  - 发送前校验手机号，发送后显示 60 秒倒计时。
  - 验证码过期、错误或请求过于频繁时显示明确反馈。
  - 验证通过后由后端签发登录 Token。
- 现状差距：当前验证码为客户端演示逻辑，存在固定值，尚未形成真实认证能力。

### UR-AUTH-04 注册

- 优先级：P0
- 当前状态：部分实现
- 需求：注册流程应收集手机号、密码、验证码、昵称、地区和角色；提交前完成协议确认及字段校验。
- 验收标准：
  - 重复手机号、验证码错误和网络失败分别显示明确错误。
  - 密码长度为 8–20 位，至少包含两类字符；前后端规则一致。
  - 注册成功后自动登录，失败时保留用户已经填写的非敏感信息。
  - 用户必须主动同意用户协议和隐私政策后才能提交。

### UR-AUTH-05 角色选择与切换

- 优先级：P0
- 当前状态：已实现基础流程
- 需求：账号支持用户、领队或双角色范围。双角色账号可在用户端与领队端之间切换，单角色账号只能进入获授权的一端。
- 验收标准：
  - 页面入口与后端账号角色权限一致。
  - 切换角色不应重新注册或丢失登录状态。
  - 越权进入另一角色页面时应阻止操作并给出说明。

### UR-AUTH-06 凭证安全

- 优先级：P0
- 当前状态：待完善
- 需求：客户端不得持久化明文密码；Token 应使用 Android Keystore 支持的加密存储，并在退出登录、账号禁用或 Token 失效时清除。
- 验收标准：
  - `SharedPreferences`、日志、崩溃信息和 Intent 中均不存在明文密码。
  - 生产环境只允许 HTTPS。
  - 敏感接口统一携带有效的 Bearer Token。

## 5.2 用户首页与导航

### UR-NAV-01 一级导航

- 优先级：P0
- 当前状态：已实现
- 需求：底部导航固定包含“路线、拼单、消息、我的”，切换时保留各页面的列表位置、筛选条件和未提交输入。
- 验收标准：连续切换页面不重复叠加 Fragment，不丢失当前选项；返回键行为符合 Android 导航习惯。

### UR-NAV-02 全局状态反馈

- 优先级：P1
- 当前状态：部分实现
- 需求：所有网络页面统一具有加载、空数据、错误和正常四种状态，并提供重试入口。
- 验收标准：弱网、断网、超时、服务端空数据均有可区分的界面反馈，不能长期停留在无说明的空白页面。

## 5.3 AI 研学路线规划

### UR-ROUTE-01 路线需求输入

- 优先级：P0
- 当前状态：已实现
- 需求：用户可用自然语言描述目的地、天数、主题、年龄、体力和偏好，也可点击示例快捷填入需求。
- 验收标准：空输入不提交；提交后立即显示用户消息并滚动到底部；规划期间禁用重复提交。

### UR-ROUTE-02 多轮规划

- 优先级：P1
- 当前状态：部分实现
- 需求：同一账号的一次规划上下文应使用稳定的会话标识，用户可以追加“减少步行”“加入博物馆”等要求迭代路线。
- 验收标准：后续指令能引用本轮已有路线；新建会话后不串用旧上下文；用户可主动清空或重开规划。

### UR-ROUTE-03 规划过程反馈

- 优先级：P0
- 当前状态：已实现基础反馈
- 需求：AI 规划可能耗时较长，客户端应分阶段展示“生成路线、加载节点、规划步行道路”等进度，并允许安全退出页面。
- 验收标准：请求期间不重复发送；Activity/Fragment 销毁后不更新失效视图；超时后显示失败原因和重试入口。

### UR-ROUTE-04 规划结果

- 优先级：P0
- 当前状态：已实现
- 需求：结果至少展示路线编号、按顺序排列的景点、推荐停留时间、备注、总时长、步行摘要和地图预览。
- 验收标准：节点按 `visitOrder` 排序；部分节点缺少坐标或备注时页面仍可用；数据无法加载时明确说明是“路线已生成但详情加载失败”。

### UR-ROUTE-05 规划失败与降级

- 优先级：P0
- 当前状态：部分实现
- 需求：区分登录失效、网络失败、AI 失败、路线 ID 缺失、节点缺失和地图规划失败。演示折线只能在开发模式使用，不能在生产环境伪装成真实路线。
- 验收标准：错误提示可指导下一步；可安全重试；失败结果不能被误发布为完整项目。

### UR-ROUTE-06 路线编辑

- 优先级：P1
- 当前状态：待实现
- 需求：发布前允许用户调整景点顺序、删除节点、修改停留时长和重新规划，并保留修改后的版本。
- 验收标准：修改后总时长、地图折线和发布数据同步刷新；用户取消编辑时恢复上一个已保存版本。

## 5.4 地图与定位

### UR-MAP-01 路线预览

- 优先级：P0
- 当前状态：已实现
- 需求：路线结果及项目详情应显示真实道路折线和途经点，支持全屏查看、缩放和返回。
- 验收标准：至少两个有效坐标时显示地图；坐标不足时隐藏地图并解释原因；全屏页与卡片页线路一致。

### UR-MAP-02 定位权限

- 优先级：P0
- 当前状态：部分实现
- 需求：首次使用定位时按需申请精确/粗略定位权限，拒绝后仍允许查看不依赖当前位置的路线。
- 验收标准：申请前说明用途；拒绝、永久拒绝和系统关闭定位均有对应引导；不因缺少定位权限崩溃。

### UR-MAP-03 高德 Key 异常

- 优先级：P0
- 当前状态：已实现基础检测
- 需求：未配置或 Key 校验失败时隐藏不可用地图，并展示配置/服务异常提示；不得影响路线文字信息和其他页面。

## 5.5 拼单发布与管理

### UR-PROJECT-01 发布设置

- 优先级：P0
- 当前状态：部分实现
- 需求：用户确认 AI 路线后，应进入发布设置并可编辑标题、出发日期、人数上限、地区、主题标签和补充说明。
- 验收标准：
  - 出发日期不得早于当前日期。
  - 人数上限必须大于当前人数，并设置合理上限。
  - 发布前二次确认关键信息。
  - 发布成功后返回项目 ID，路线卡片标记为“已发布”，禁止重复发布。
- 现状差距：当前标题截取自路线文本，日期固定为七天后，人数固定为 10，缺少发布表单和防重复机制。

### UR-PROJECT-02 项目列表

- 优先级：P0
- 当前状态：已实现
- 需求：项目卡片展示标题、地区、日期、主题、状态、当前/最大人数、发起人及是否已有领队；列表支持下拉刷新。
- 验收标准：状态排序顺序为招募中、匹配中、已确认、进行中、已完成、已取消；加载后卡片信息与详情一致。

### UR-PROJECT-03 项目筛选

- 优先级：P1
- 当前状态：已实现基础筛选
- 需求：支持关键词、地区、主题、状态、出发日期范围、是否已有领队、仅可加入等条件；已选条件可回显和一键重置。
- 验收标准：日期范围合法；重置后恢复全量列表；明确采用“任一条件命中”还是“全部条件命中”，客户端与后端保持一致。当前项目按任一条件命中显示。

### UR-PROJECT-04 项目详情

- 优先级：P0
- 当前状态：已实现
- 需求：详情展示项目基本信息、发起人、人数、状态、路线节点、推荐时长、地图、领队信息及当前用户可执行操作。
- 验收标准：详情数据刷新后再判断按钮状态；接口部分失败不影响其他已加载区域；本人发布的项目不显示“加入”。

### UR-PROJECT-05 加入项目

- 优先级：P0
- 当前状态：已实现基础流程
- 需求：非发起人可在 `OPEN` 或 `MATCHING` 且未满员时加入。后端必须原子校验重复加入、人数上限和项目状态。
- 验收标准：
  - 重复点击只产生一次加入结果。
  - 加入成功后立即刷新人数和按钮状态。
  - 已加入用户再次进入详情显示“已加入”。
  - 满员或状态变化导致失败时，以后端最新结果刷新详情。

### UR-PROJECT-06 退出与取消

- 优先级：P0
- 当前状态：待实现
- 需求：普通参与者可在允许的阶段退出；发起人可取消未开始项目。系统必须定义截止时间、是否需要领队确认以及退出后会话权限。
- 验收标准：退出后人数减少且不能继续访问受限项目会话；取消时通知全部成员和领队；`IN_PROGRESS`、`DONE` 状态不可直接退出或取消。

### UR-PROJECT-07 我的项目

- 优先级：P1
- 当前状态：部分实现
- 需求：“我的”中分别展示我发起、我加入、进行中和已结束的项目，可快速进入详情、会话或历史路线。

## 5.6 用户与领队匹配

### UR-MATCH-01 领队接单结果

- 优先级：P0
- 当前状态：领队端已实现接单，用户端展示部分实现
- 需求：领队仅能接取没有领队且状态为 `OPEN`/`MATCHING` 的项目；接单成功后用户端应展示领队资料并收到通知。
- 验收标准：并发接单由后端保证只有一个领队成功；失败方刷新项目状态；接单成功后项目进入约定状态并可建立项目会话。

### UR-MATCH-02 项目确认

- 优先级：P0
- 当前状态：待实现
- 需求：达到成团条件且已有领队后，由发起人和领队完成确认，项目从 `MATCHING` 进入 `CONFIRMED`。
- 验收标准：确认前展示日期、人数、领队和路线摘要；双方确认结果可追踪；确认后禁止普通加入并通知全部成员。

### UR-MATCH-03 开始与完成

- 优先级：P0
- 当前状态：待实现
- 需求：领队在已确认且达到出发条件时开始活动，结束后完成项目；关键操作应要求二次确认并记录操作者和时间。

## 5.7 消息与通知

### UR-CHAT-01 项目会话

- 优先级：P0
- 当前状态：部分实现
- 需求：用户加入已有领队的项目或领队接单后，系统创建或复用与项目绑定的会话，至少支持项目群聊。
- 验收标准：同一项目和成员组合不重复创建会话；未加入项目的用户不可读取会话；退出或取消后的权限按业务规则收回。

### UR-CHAT-02 文本消息

- 优先级：P0
- 当前状态：部分实现（主要为本地 SQLite）
- 需求：消息应通过后端实时或准实时同步，并包含消息 ID、会话 ID、发送者、发送时间、发送状态和已读状态。
- 验收标准：发送中、成功、失败可见；失败消息可重试；不同设备能收到同一会话消息；消息按服务端时间稳定排序。

### UR-CHAT-03 会话列表

- 优先级：P0
- 当前状态：已实现本地列表
- 需求：会话列表展示头像、名称、项目标识、最后一条消息、时间和未读数，按最后消息时间倒序排列。
- 验收标准：进入会话后未读数清零；99 条以上显示 `99+`；空列表不自动插入生产环境演示会话。

### UR-CHAT-04 业务通知

- 优先级：P1
- 当前状态：待实现
- 需求：加入成功、领队接单、项目确认、日期或路线变更、开始、完成和取消均应产生站内通知；重要事件可结合系统推送。

## 5.8 个人资料与偏好

### UR-PROFILE-01 资料展示与编辑

- 优先级：P1
- 当前状态：已实现基础能力
- 需求：用户可查看和修改头像、昵称、地区和个人简介；保存成功后所有页面同步更新。
- 验收标准：昵称长度和敏感字符受限；头像支持相册/相机、裁剪、压缩和上传；上传失败保留原头像。

### UR-PROFILE-02 研学偏好

- 优先级：P1
- 当前状态：已实现基础能力
- 需求：用户可从历史人文、博物馆、非遗、科技、自然、地理、航天、农耕、艺术、红色教育、高校、职业、英语、摄影和亲子等标签中选择偏好。
- 验收标准：偏好从后端读取并可更新；重新进入页面时选择状态一致；筛选和推荐明确说明是否使用偏好。

### UR-PROFILE-03 历史路线

- 优先级：P1
- 当前状态：部分实现
- 需求：用户可查看已参加、已完成或由自己创建的历史路线，打开详情并查看地图；已取消项目单独标识。

## 5.9 设置与账号管理

### UR-SET-01 自动登录与通知开关

- 优先级：P1
- 当前状态：自动登录已实现，通知部分待实现
- 需求：开关状态持久化；关闭自动登录不立即退出，但下次冷启动需重新登录；通知开关与系统权限状态保持一致。

### UR-SET-02 清理缓存

- 优先级：P2
- 当前状态：已实现基础交互
- 需求：清理图片、网络临时文件等可再生缓存，不得删除账号凭证、用户资料和聊天记录；操作前显示预计大小，完成后显示释放空间。

### UR-SET-03 退出登录

- 优先级：P0
- 当前状态：已实现
- 需求：用户确认退出后清除 Token、账号会话及敏感缓存，返回登录入口并清空业务页面返回栈。

### UR-SET-04 协议、隐私与版本

- 优先级：P0
- 当前状态：部分实现
- 需求：提供可访问的用户协议、隐私政策、权限说明和版本信息；隐私政策应披露高德 SDK、定位、相机、相册和网络数据用途。

## 6. 项目状态与操作规则

| 状态值 | 用户展示 | 用户可执行 | 领队可执行 | 退出条件 |
|---|---|---|---|---|
| `OPEN` | 招募中 | 查看、加入、发起人编辑/取消 | 查看、接单 | 领队接单或进入匹配；发起人取消 |
| `MATCHING` | 匹配中 | 查看、按规则加入/退出、发起人确认 | 查看、接单或确认 | 双方确认、取消或退回招募 |
| `CONFIRMED` | 已确认 | 查看路线、聊天、接收通知 | 查看成员、沟通、开始活动 | 开始或异常取消 |
| `IN_PROGRESS` | 进行中 | 查看路线和领队、聊天 | 带队、查看步行指引、结束活动 | 完成或异常终止 |
| `DONE` | 已完成 | 查看历史、后续评价 | 查看历史 | 终态 |
| `CANCELLED` | 已取消 | 查看取消原因 | 查看取消原因 | 终态 |

所有状态变更必须由后端校验当前状态、操作者角色和项目版本，避免重复提交或并发覆盖。

## 7. 数据与接口要求

### 7.1 核心数据

- 账号：账号 ID、角色、昵称、手机号、地区、头像、状态。
- 路线：路线 ID、节点顺序、名称、经纬度、推荐时长、备注。
- 项目：项目 ID、路线 ID、发起人、领队、地区、标签、标题、日期、人数、状态和时间戳。
- 会话：会话 ID、项目 ID、成员 ID、领队 ID。
- 消息：消息 ID、会话 ID、发送者、内容、时间、状态和已读信息。

### 7.2 接口一致性

- 所有接口使用统一结果结构和错误码，客户端不得仅依赖提示文本判断业务结果。
- 列表接口应支持分页、稳定排序和刷新游标，避免重复项或漏项。
- 加入、退出、接单、确认、开始、完成、取消必须具备幂等性和并发控制。
- 项目详情返回当前用户与项目的关系，例如 `OWNER`、`MEMBER`、`NOT_JOINED`，避免客户端仅凭本地状态推断按钮。
- 生产环境必须使用 HTTPS，后端地址应按开发、测试、生产构建环境配置。

## 8. 非功能需求

### 8.1 性能

- 普通页面首屏在正常网络下 2 秒内出现有效内容或骨架屏。
- 列表滚动无明显卡顿；图片按尺寸压缩并缓存。
- AI 路线属于长任务，应展示持续进度，120 秒超时后可重试。

### 8.2 稳定性

- 网络中断、接口空数据、地图 Key 异常、定位拒绝和屏幕旋转不得导致崩溃。
- 异步回调必须遵守 Activity/Fragment 生命周期。
- 关键操作失败后按钮恢复可用，且不会产生重复项目、重复成员或重复领队。

### 8.3 安全与隐私

- 不保存、打印或上传明文密码。
- Token、手机号、定位和聊天内容按敏感数据处理。
- 相机、相册、定位均按需申请，并提供拒绝后的降级体验。
- 日志不得包含 Token、密码、完整手机号或高德 Key。

### 8.4 兼容性与易用性

- 支持 Android 8.0（API 26）至目标 SDK 36。
- 适配全面屏、刘海屏、系统字体缩放和常见分辨率。
- 关键按钮具备可访问性描述；文本与背景满足基本对比度要求。
- 所有用户可见文本使用资源文件，避免硬编码及乱码。

## 9. 埋点与监控建议

- 注册开始/成功/失败、登录成功/失败、角色切换。
- AI 规划提交、耗时、成功、失败类型、重试。
- 路线地图打开、发布开始/成功/失败。
- 筛选使用情况、项目详情打开、加入成功/失败。
- 领队接单、项目状态转换和聊天会话创建。
- 崩溃、HTTP 错误率、AI 超时率和地图服务失败率。

埋点不得包含用户原始密码、Token、完整聊天内容或精确位置轨迹。

## 10. 当前版本主要差距与建议顺序

### P0：先补齐业务闭环和安全

1. 移除固定验证码与明文密码存储，接入真实验证码和安全凭证存储。
2. 增加可编辑的拼单发布表单与防重复发布。
3. 增加“我的项目”、退出项目、发起人取消项目。
4. 补齐 `MATCHING → CONFIRMED → IN_PROGRESS → DONE/CANCELLED` 的后端接口和客户端操作。
5. 将聊天从本地演示数据升级为服务端消息同步。
6. 将后端服务切换为 HTTPS 和多环境构建配置。

### P1：完善体验与信息一致性

1. 增加路线编辑和真正的多轮规划。
2. 统一加载、空态、错误和重试组件。
3. 增加项目通知和领队资料展示。
4. 完善历史路线、偏好推荐与项目关系字段。

### P2：增强运营与服务能力

1. 项目评价、举报和客服反馈。
2. 行程分享、日历提醒和集合签到。
3. 更完整的无障碍、国际化和数据分析能力。

## 11. 发布验收门槛

版本进入正式测试前至少满足：

- P0 需求全部完成并通过主流程回归。
- 用户可完成“注册/登录 → 规划路线 → 发布项目 → 领队接单 → 用户加入 → 会话沟通 → 确认 → 开始 → 完成”的完整链路。
- 并发加入和并发接单测试不超员、不重复、不产生双领队。
- 无固定验证码、明文密码、本地演示会话和演示地图折线进入生产包。
- HTTPS、Token 失效处理、权限拒绝和弱网场景通过专项测试。
- Android 8.0、主流 Android 版本及至少一种全面屏设备完成兼容测试。
