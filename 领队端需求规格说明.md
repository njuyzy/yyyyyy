# 领队端需求规格说明书

> 本文件描述研学导览 App **领队端**应实现的全量目标功能。范围仅限领队端；用户端见 [用户端需求规格说明.md](./用户端需求规格说明.md)，业务状态见 [用户与领队交互状态图.md](./用户与领队交互状态图.md)。

---

## 1. 文档说明

### 1.1 目的与背景
统一领队端的功能边界、行为约定与验收标准，作为开发、测试与验收的共同依据。领队端服务于"带队者"角色：浏览并接取研学拼单、查看路线与地图、与成员沟通、执行带队流程、管理个人资料与历史。

### 1.2 范围
覆盖领队端的全部目标功能（含已实现与待实现），逐条标注实现状态。不含用户端专属功能（AI 路线规划、发布拼单、加入项目等），仅在交互边界处引用。

### 1.3 读者对象
客户端开发、后端开发、测试、产品与项目验收人员。

### 1.4 术语与缩写
| 术语 | 说明 |
|---|---|
| 领队 / Leader | 接取并带领研学项目的角色；账号 `role` 为 `LEADER` 或 `BOTH` |
| 双角色 / BOTH | 同一账号同时具备用户与领队身份，可在两端切换 |
| 项目 / Project | 一次研学拼单，含标题、地区、日期、人数、标签、状态、路线等 |
| 订单 / 接单 | 领队视角下"可接项目"与"接取项目"的业务称呼 |
| 路线节点 / RouteNode | 项目路线中的一个站点（景点/场馆），含名称、坐标、停留时长等 |
| 步行规划 | 调用高德 `RouteSearch` 逐段计算相邻节点间步行路径 |
| 项目状态 | `OPEN`、`MATCHING`、`CONFIRMED`、`IN_PROGRESS`、`DONE`、`CANCELLED` |

### 1.5 参考文档
- `用户与领队交互状态图.md`：项目生命周期与状态转换权限。
- `用户端需求规格说明.md`：用户端功能与交互边界。

---

## 2. 领队端总体概述

### 2.1 领队角色定义与权限
- 登录后依据账号 `role` 确定角色范围（`ROLE_SCOPE`）：`LEADER` 仅领队、`BOTH` 双角色、`USER` 仅用户。
- 仅 `BOTH` 账号在"我的"中显示"切换模式"入口，可在用户端与领队端之间切换。
- 领队核心权限：浏览可接项目、接取无领队项目、查看路线与地图、带队状态流转（目标）、参与项目群沟通。

### 2.2 端内导航结构
领队端主界面 `LeaderMainActivity` 采用底部导航（`bottom_nav_leader`），三个主页面：
1. **接单**：可接项目列表与筛选。
2. **消息**：会话列表与聊天。
3. **我的**：领队个人中心。

### 2.3 与用户端、后端的关系
- 与用户端共享后端 `UserService`（Retrofit）与鉴权体系（`ApiClient` 统一注入 `Bearer` Token）。
- 领队接单、状态流转须由后端原子处理；客户端按钮禁用仅防重复点击，不替代并发控制。
- 接单成功后应为项目成员创建/复用项目群会话并通知用户（目标）。

### 2.4 实现状态图例
- ✅ 已实现：当前代码已具备该行为。
- 🟡 部分实现：主流程存在但有缺口（如仅本地、字段不全、无并发保障）。
- ⬜ 待实现：目标功能，当前无对应实现。

每个功能条目在"实现状态"中给出图标与关键代码位置（如有）。

---

## 3. 功能需求

> 每个功能采用统一 SRS 字段：实现状态 / 功能描述 / 参与者 / 前置条件 / 操作流程 / 输入·输出 / 异常处理 / 接口依赖 / 验收标准。

### 3.1 身份与账号

#### F-3.1-01 领队登录与角色识别
- **实现状态**：✅ `MainActivity.java:231`（performLogin），`RoleSelectionActivity`
- **功能描述**：领队通过手机号+密码登录，系统按账号 `role` 确定角色范围并落地登录态。
- **参与者**：领队、后端。
- **前置条件**：账号已注册且具备领队或双角色权限。
- **操作流程**：
  1. 领队输入手机号、密码，点击登录。
  2. 调用登录接口，成功后保存 `account_id`、`user_inf`、`region_code`、`ROLE_SCOPE`、`Mode`、`is_logged_in` 及 Token。
  3. `role=LEADER` → `Mode=LEADER`；`role=BOTH` → 默认 `Mode=LEADER`；据 `Mode` 跳转 `LeaderMainActivity`。
- **输入·输出**：手机号、密码 → 登录结果、账号信息、Token；跳转领队主页。
- **异常处理**：密码错误提示；网络/超时提示；返回体缺失提示"网络错误"。
- **接口依赖**：`POST login`。
- **验收标准**：领队账号登录后进入领队端；双角色账号默认进入领队端；错误凭证有明确提示且不跳转。

#### F-3.1-02 用户端 ↔ 领队端角色切换
- **实现状态**：✅ 领队→用户 `leader/fragment/profile/profile.java:290`；用户→领队 `user/fragment/profile/profile.java:207`
- **功能描述**：双角色账号在"我的"中切换当前端，切换后进入对应主界面。
- **参与者**：领队/双角色用户。
- **前置条件**：`ROLE_SCOPE=BOTH`；已登录。
- **操作流程**：
  1. 在"我的"点击"切换模式"。
  2. 写入 `Mode`（`LEADER`/`USER`）。
  3. 启动目标主界面并 `finish()` 当前主界面。
- **输入·输出**：无输入 → `Mode` 更新、界面切换。
- **异常处理**：非双角色隐藏入口；点击时二次校验"当前身份不可切换"。
- **接口依赖**：无（仅本地状态）。
- **验收标准**：双角色可来回切换且不丢失登录态；单角色不显示入口；切换后不得因首屏鉴权请求异常而被登出或崩溃（见非功能需求 5.3）。

#### F-3.1-03 登录态保持与失效处理
- **实现状态**：✅ `ApiClient.java`（Token 注入/存取），`SessionHelper.java`（isLoggedIn/handleUnauthorized）
- **功能描述**：请求自动携带 Token；登录失效（401）时清理登录态并返回登录页。
- **参与者**：领队、系统、后端。
- **前置条件**：已登录并持有 Token。
- **操作流程**：拦截器为请求加 `Authorization: Bearer <token>`；接口返回 401 时清理 `is_logged_in`/`account_id`/Token 并跳登录页。
- **输入·输出**：Token → 鉴权请求；401 → 登出并跳转。
- **异常处理**：Token 缺失时视为未登录，展示"请先登录"而非强制跳转（列表类被动加载场景，见 5.3）。
- **接口依赖**：全部鉴权接口。
- **验收标准**：Token 有效期内免重复登录；失效后能安全回到登录流程且提示清晰。

### 3.2 领队首页与导航

#### F-3.2-01 底部导航与页面切换
- **实现状态**：✅ `LeaderMainActivity.java`
- **功能描述**：接单/消息/我的三页通过底部导航切换，采用 add+hide/show 保活。
- **参与者**：领队。
- **前置条件**：已进入领队主界面。
- **操作流程**：点击底部项 → 切换对应 Fragment；首次进入默认"接单"。
- **输入·输出**：导航点击 → Fragment 显示。
- **异常处理**：Fragment 未创建时创建，已存在则显示，避免重复添加。
- **接口依赖**：无。
- **验收标准**：三页切换流畅、状态保留；默认停留"接单"。

### 3.3 接单浏览与筛选

#### F-3.3-01 可接项目列表与刷新
- **实现状态**：✅ `leader/fragment/order/orderList.java:518`（loadOrders）
- **功能描述**：拉取项目列表，展示标题、城市、日期、人数、标签、状态、发起人、路线摘要与时长，支持下拉刷新。
- **参与者**：领队、后端。
- **前置条件**：已登录（`SessionHelper.isLoggedIn`）。
- **操作流程**：
  1. 进入或下拉时校验登录态。
  2. 拉取较宽结果集（`getProjects(accountId,1,100)`）。
  3. 本地按筛选条件"任一命中"过滤（`ProjectUiHelper.filterProjectsByAnyMatch`）。
  4. 排序后逐项补充发起人昵称、路线摘要与时长（`getAccount`、`getRouteNodes`）。
- **输入·输出**：accountId、分页 → 项目卡片列表。
- **异常处理**：未登录展示"请先登录"空态；401 提示并登出；失败/网络错误展示对应空态并可下拉重试；空结果展示"暂无符合条件的路线"。
- **接口依赖**：`GET projects`、`GET accounts/{id}`、`GET routes/{id}`。
- **验收标准**：列表可正常加载与刷新；筛选生效；各失败态有对应文案。

#### F-3.3-02 标签快捷筛选（横向双行）
- **实现状态**：✅ `orderList.setupTagGrid`，`TagGridAdapter.java`，`item_tag_card.xml`
- **功能描述**：标签以横向 2 行、每屏 5 列展示，可多选筛选，右滑查看更多标签，图标随列宽自适应。
- **参与者**：领队。
- **前置条件**：列表页已加载。
- **操作流程**：点击标签切换选中 → 触发重新筛选。
- **输入·输出**：选中标签集合 → 过滤后的列表。
- **异常处理**：无匹配时展示空态。
- **接口依赖**：无（本地过滤）；标签取自 `R.array.route_tag_names`。
- **验收标准**：标签多选与取消即时生效；横向滚动展示全部标签；图标不裁切、不溢出固定宽度。

#### F-3.3-03 高级筛选（地区/状态/关键词/日期/领队情况/仅可接）
- **实现状态**：✅ `orderList.setupFilterSheet`
- **功能描述**：底部弹窗提供关键词、省市联动地区、标签、项目状态、领队情况、仅显示可接、出发日期区间等条件，确认后过滤。
- **参与者**：领队、后端（地区数据）。
- **前置条件**：列表页已加载。
- **操作流程**：打开筛选 → 选择条件 → 确定应用/重置清空。
- **输入·输出**：各筛选字段 → 过滤后的列表并回填筛选态。
- **异常处理**：地区加载失败时静默降级；日期选择器规范格式。
- **接口依赖**：`GET regions/provinces`、`GET regions/{adcode}/children`。
- **验收标准**：各条件单独与组合筛选均生效；重置可清空全部条件；省市联动正确。

#### F-3.3-04 当前城市展示
- **实现状态**：✅ `orderList.loadCurrentCity`
- **功能描述**：顶部依据本地 `region_code` 展示当前城市，无法解析时展示"全国"。
- **参与者**：领队。
- **前置条件**：本地存在地区码（可为空）。
- **操作流程**：读取 `region_code` → 解析为城市名或"全国"。
- **输入·输出**：region_code → 城市文案。
- **异常处理**：解析失败回退"全国"。
- **接口依赖**：无。
- **验收标准**：有地区时显示对应城市，否则显示"全国"。

#### F-3.3-05 站内搜索
- **实现状态**：⬜（`orderList` 搜索按钮当前为"搜索功能开发中"占位）
- **功能描述**：按关键词跨项目标题/景点搜索并展示结果页。
- **参与者**：领队、后端。
- **前置条件**：已登录。
- **操作流程**：点击搜索 → 进入搜索页 → 输入关键词 → 返回结果。
- **输入·输出**：关键词 → 搜索结果列表。
- **异常处理**：空关键词提示；无结果空态；网络错误重试。
- **接口依赖**：建议 `GET projects/filter`（keyword）或专用搜索接口。
- **验收标准**：能按关键词返回匹配项目并进入详情。

### 3.4 项目/订单详情

#### F-3.4-01 项目信息与路线详情展示
- **实现状态**：✅ `leader/orderDetailActivity.java`
- **功能描述**：展示标题、城市·出发日期·人数、发起人、状态徽标、按访问顺序排列的路线节点（名称、推荐时长、备注）。
- **参与者**：领队、后端。
- **前置条件**：从列表进入并传入项目数据（`EXTRA_PROJECT_JSON`）。
- **操作流程**：
  1. 解析项目 → 绑定头部信息。
  2. 拉取最新项目（`getProject`）刷新状态与按钮。
  3. 拉取发起人昵称（`getAccount`）与路线节点（`getRouteNodes`）并排序展示。
- **输入·输出**：项目 JSON / 项目 ID → 详情界面。
- **异常处理**：项目数据无效时提示并退出；路线为空/加载失败展示"暂无/加载失败"并隐藏地图区。
- **接口依赖**：`GET projects/{id}`、`GET accounts/{id}`、`GET routes/{id}`。
- **验收标准**：详情各字段正确；路线按顺序展示；状态与接单按钮与最新数据一致。

#### F-3.4-02 接单按钮状态机
- **实现状态**：✅ `orderDetailActivity.setupAcceptButton`
- **功能描述**：依据是否已有领队、项目状态与当前账号，动态显示"接单/已接单/已有领队/已确认/进行中/已完成/已取消/暂不可接"。
- **参与者**：领队。
- **前置条件**：已加载项目详情。
- **操作流程**：读取 `leaderAccountId` 与状态 → 计算按钮文案与可点击性。
- **输入·输出**：项目状态、领队 ID、当前账号 → 按钮状态。
- **异常处理**：不可接状态一律禁用点击。
- **接口依赖**：无（基于已加载数据）。
- **验收标准**：各状态下按钮文案与可点击性符合状态权限表（见第 4 节）。

### 3.5 接单

#### F-3.5-01 领队接取项目
- **实现状态**：✅ 客户端 `orderDetailActivity.acceptOrder`；🟡 后端原子分配与并发保障需保证
- **功能描述**：领队接取无领队且可接的项目，成功后项目归属该领队。
- **参与者**：领队、后端。
- **前置条件**：已登录；项目无领队；状态允许接单；账号具备领队权限。
- **操作流程**：
  1. 校验登录态与 accountId。
  2. 禁用按钮，调用 `assignLeader(projectId, leaderAccountId)`。
  3. 成功 → 提示"接单成功"、按钮置"已接单"、更新归属、`setResult(RESULT_OK)`。
- **输入·输出**：projectId、leaderAccountId → 分配结果。
- **异常处理**：401 登出；失败恢复按钮并提示后端消息；网络错误提示；并发被他人抢占时刷新详情并提示。
- **接口依赖**：`POST projects/{id}/leader`。
- **验收标准**：合法条件下可接单成功；并发场景下同一项目最终仅一名领队；失败可重试。

#### F-3.5-02 防重复接单与权限校验
- **实现状态**：🟡（客户端按钮禁用；后端原子性与去重需保证）
- **功能描述**：阻止已被接取/已完成/已取消项目再次接单，阻止非领队权限账号接单。
- **参与者**：领队、后端。
- **前置条件**：进入接单动作。
- **操作流程**：客户端按状态禁用；后端在事务内校验状态与权限后原子写入。
- **输入·输出**：接单请求 → 通过/拒绝。
- **异常处理**：拒绝时返回明确原因并同步最新项目。
- **接口依赖**：`POST projects/{id}/leader`（后端事务）。
- **验收标准**：非法接单被后端拒绝；客户端展示最新状态。

### 3.6 路线与地图

#### F-3.6-01 高德步行路线规划（摘要）
- **实现状态**：✅ `LeaderWalkRoutePlanner.java`，`orderDetailActivity.startRoutePlanning`
- **功能描述**：详情页加载路线后，调用高德逐段步行规划，得到总时长/总距离摘要、分段文字指引与沿道路折线。
- **参与者**：领队、高德服务。
- **前置条件**：已配置高德 Key；路线含≥1 有效坐标。
- **操作流程**：排序节点 → 逐段 `calculateWalkRouteAsyn` → 累计距离/时长、拼接折线与指引 → 回调摘要卡片。
- **输入·输出**：节点坐标序列 → 摘要、分段指引、道路折线点。
- **异常处理**：某段失败回退直线并标注；`RouteSearch` 初始化失败整体回退直连；坐标不足提示；部分失败给出提示。
- **接口依赖**：高德 `RouteSearch`（WalkRoute）。
- **验收标准**：多节点项目能给出合理步行摘要与分段指引；失败段落有降级与提示。

#### F-3.6-02 全屏地图查看真实规划路线
- **实现状态**：✅ `orderDetailActivity.openFullscreenRouteMap` + `RouteMapFullscreenActivity`
- **功能描述**：点击地图区进入全屏地图，绘制高德规划的真实道路路线并在各站点打标记（起点/途经/终点）。
- **参与者**：领队、高德服务。
- **前置条件**：已配置高德 Key；已有规划折线或至少节点坐标。
- **操作流程**：优先传规划道路折线 + 节点站点 → 全屏页绘制折线与标记并自适应镜头；未规划完成时回退按站点连线。
- **输入·输出**：道路折线、站点坐标 → 全屏地图。
- **异常处理**：无坐标提示；Key 缺失提示并退出；地图初始化失败提示；地图生命周期须正确管理，避免销毁崩溃（见 5.3）。
- **接口依赖**：高德地图 SDK；折线来源见 F-3.6-01。
- **验收标准**：进入全屏地图即显示真实规划路线与站点标记，而非直线连接。

#### F-3.6-03 分段步行指引详情
- **实现状态**：✅ `WalkRouteDetailActivity`，`WalkSegmentListAdapter`
- **功能描述**：展示各段步行文字指引与总摘要。
- **参与者**：领队。
- **前置条件**：已生成分段指引。
- **操作流程**：从摘要卡进入 → 列表展示分段指引。
- **输入·输出**：指引列表、摘要 → 详情页。
- **异常处理**：无指引时不进入。
- **接口依赖**：无（数据来自 F-3.6-01）。
- **验收标准**：能查看逐段文字指引与整体摘要。

#### F-3.6-04 地图能力降级
- **实现状态**：✅ `hasConfiguredAmapKey`（详情页与全屏页）
- **功能描述**：未配置高德 Key 或坐标不足时隐藏地图区/提示，避免崩溃。
- **参与者**：领队、系统。
- **前置条件**：进入含地图的页面。
- **操作流程**：检测 Key 与坐标 → 不满足则降级。
- **输入·输出**：Key/坐标状态 → 是否展示地图。
- **异常处理**：给出可读提示（如"未配置高德 Key"）。
- **接口依赖**：`PackageManager` 读取 meta-data。
- **验收标准**：无 Key/无坐标时页面稳定、有提示、不崩溃。

### 3.7 带队执行（项目状态流转）

#### F-3.7-01 双方确认（MATCHING → CONFIRMED）
- **实现状态**：⬜（无确认接口与操作页面）
- **功能描述**：领队与发起人分别确认后项目进入待出发。
- **参与者**：领队、发起人、后端。
- **前置条件**：项目已有领队；满足成团条件。
- **操作流程**：领队在详情/项目页点击"确认" → 后端记录领队确认 → 双方均确认后置 `CONFIRMED`。
- **输入·输出**：确认动作 → 项目状态与"待确认方"展示。
- **异常处理**：仅单方确认时展示仍待确认的一方；重复确认幂等。
- **接口依赖**：建议 `POST projects/{id}/confirm`（领队）。
- **验收标准**：双方确认后状态变更并同步双端；未满足条件时给出原因。

#### F-3.7-02 开始活动（CONFIRMED → IN_PROGRESS）
- **实现状态**：⬜
- **功能描述**：到达允许开始时间后，当前领队开始活动。
- **参与者**：当前领队、后端。
- **前置条件**：项目 `CONFIRMED`；到达可开始时间。
- **操作流程**：领队点击"开始活动" → 后端置 `IN_PROGRESS` 并通知成员。
- **输入·输出**：开始动作 → 状态更新与开始通知。
- **异常处理**：未到时间/非当前领队拒绝并提示。
- **接口依赖**：建议 `POST projects/{id}/start`。
- **验收标准**：满足条件可开始并同步用户端为"活动中"。

#### F-3.7-03 完成活动（IN_PROGRESS → DONE）
- **实现状态**：⬜
- **功能描述**：活动结束后领队确认完成，进入终态并沉淀历史。
- **参与者**：当前领队、后端。
- **前置条件**：项目 `IN_PROGRESS`。
- **操作流程**：领队点击"结束活动" → 后端置 `DONE` → 同步历史与完成通知。
- **输入·输出**：完成动作 → 状态更新、历史记录。
- **异常处理**：非当前领队/状态不符拒绝；允许重试。
- **接口依赖**：建议 `POST projects/{id}/complete`。
- **验收标准**：完成后进入 `DONE` 终态并出现在历史路线。

#### F-3.7-04 异常取消与退出
- **实现状态**：⬜
- **功能描述**：出发前异常取消需二次确认并填写原因；领队在未确认前可按规则退出，项目回到 `OPEN`。
- **参与者**：领队、发起人、管理员、后端。
- **前置条件**：符合取消/退出规则的状态。
- **操作流程**：填写原因并二次确认 → 后端置 `CANCELLED` 或回退 `OPEN` → 通知相关方并记录审计。
- **输入·输出**：原因、动作 → 状态更新、通知、审计。
- **异常处理**：不满足规则拒绝并保留原状态。
- **接口依赖**：建议 `POST projects/{id}/cancel`、`POST projects/{id}/leader/quit`。
- **验收标准**：取消/退出记录操作者、时间、原因并同步各方；终态不可被普通端重开。

### 3.8 消息与协同

#### F-3.8-01 会话列表与聊天
- **实现状态**：🟡 `Chat/fragment/ConversationList`、`Chat/chatActivity`（本地 SQLite，含演示数据）
- **功能描述**：展示会话列表（最后一条消息、未读数），支持单聊/群聊基础页面。
- **参与者**：领队、成员。
- **前置条件**：存在会话数据。
- **操作流程**：进入消息页 → 列表 → 进入会话收发消息。
- **输入·输出**：会话/消息 → 列表与聊天界面。
- **异常处理**：无会话空态；本地读写失败降级。
- **接口依赖**：目标应接入服务端消息同步接口。
- **验收标准**：能查看会话与消息；目标态下消息经服务端同步、去除演示数据。

#### F-3.8-02 项目群会话创建/复用
- **实现状态**：🟡（用户加入已有领队项目时尝试创建；领队接单后自动建群为目标）
- **功能描述**：领队接单后为项目成员创建或复用项目群会话，作为行程沟通载体。
- **参与者**：领队、成员、后端。
- **前置条件**：项目已匹配领队。
- **操作流程**：接单成功 → 后端创建/复用项目会话 → 双端可见。
- **输入·输出**：项目/成员 → 项目群会话。
- **异常处理**：已存在则复用，避免重复建群。
- **接口依赖**：`POST chat/sessions`（及后端会话同步）。
- **验收标准**：接单后成员与领队进入同一项目群且不重复创建。

### 3.9 个人中心与资质

#### F-3.9-01 领队资料查看与编辑
- **实现状态**：✅ `leader/fragment/profile/profile.java`，`PersonalInfoActivity`，头像上传/本地缓存
- **功能描述**：展示与编辑头像、昵称、地区、简介等资料。
- **参与者**：领队、后端。
- **前置条件**：已登录。
- **操作流程**：进入"我的" → 查看资料 → 编辑昵称/简介/头像 → 保存并上传。
- **输入·输出**：资料字段、头像 → 更新结果与展示。
- **异常处理**：地区获取失败提示；头像上传失败提示；无地区时回退默认文案。
- **接口依赖**：`GET accounts/{id}`、`POST accounts/{id}/username`、`POST accounts/{id}/intro`、`uploadAvatar`。
- **验收标准**：资料可查看与修改并持久化；头像更新即时可见。

#### F-3.9-02 研学主题偏好
- **实现状态**：🟡（接口已定义 `getTagPrefs`/`updateTagPrefs`）
- **功能描述**：领队设置擅长/关注的研学主题标签，用于推荐与展示。
- **参与者**：领队、后端。
- **前置条件**：已登录。
- **操作流程**：进入偏好设置 → 勾选标签 → 保存。
- **输入·输出**：标签偏好 → 保存结果。
- **异常处理**：保存失败提示并保留选择。
- **接口依赖**：`GET/POST accounts/{id}/tagPrefs`。
- **验收标准**：偏好可保存并回显。

#### F-3.9-03 领队资质认证
- **实现状态**：⬜
- **功能描述**：展示与提交领队认证（资质状态、擅长主题、带队次数），供用户判断。
- **参与者**：领队、后端、审核。
- **前置条件**：已登录。
- **操作流程**：提交认证资料 → 审核 → 展示认证状态与统计。
- **输入·输出**：资质材料 → 认证状态。
- **异常处理**：审核未过/材料缺失提示。
- **接口依赖**：`GET accounts/{id}/leaderProfile`（读取）及建议的认证提交接口。
- **验收标准**：资质状态与统计可展示，未认证有引导。

#### F-3.9-04 评价查看
- **实现状态**：🟡 `leader/fragment/profile/review.java`（入口存在）
- **功能描述**：领队查看收到的用户评价。
- **参与者**：领队。
- **前置条件**：已登录。
- **操作流程**：在"我的"进入评价页查看列表。
- **输入·输出**：评价数据 → 列表。
- **异常处理**：无评价空态。
- **接口依赖**：建议评价查询接口。
- **验收标准**：能查看历史评价（目标态接入服务端数据）。

### 3.10 历史与评价

#### F-3.10-01 历史带队路线
- **实现状态**：✅ `LeaderHistoryRouteActivity`
- **功能描述**：展示领队的历史带队/路线记录。
- **参与者**：领队、后端。
- **前置条件**：已登录。
- **操作流程**：从"我的"进入 → 加载历史列表 → 查看详情。
- **输入·输出**：账号 → 历史记录列表。
- **异常处理**：401 登出；空/失败空态。
- **接口依赖**：历史/项目查询接口。
- **验收标准**：能查看历史记录并进入详情。

#### F-3.10-02 用户对领队评价/评分
- **实现状态**：⬜
- **功能描述**：项目完成后用户对领队评分与评价，形成领队评分展示。
- **参与者**：用户、领队、后端。
- **前置条件**：项目 `DONE`。
- **操作流程**：用户提交评价 → 后端聚合 → 领队端与详情页展示评分。
- **输入·输出**：评分、评语 → 聚合评分。
- **异常处理**：重复评价幂等；非参与者不可评价。
- **接口依赖**：建议评价提交/查询接口。
- **验收标准**：完成项目可评价；领队评分正确聚合展示。

---

## 4. 项目状态流转与权限（领队视角）

领队相关的状态转换与权限（裁剪自 `用户与领队交互状态图.md` 第 3 节）：

| 当前状态 | 触发动作 | 前置条件 | 目标状态 | 失败处理 | 实现状态 |
|---|---|---|---|---|---|
| `OPEN`/`MATCHING` | 接单 | 无现有领队、账号具备领队权限 | `MATCHING` | 并发失败刷新项目 | ✅/🟡 |
| `MATCHING` | 双方确认 | 已有领队、满足成团条件 | `CONFIRMED` | 显示仍待确认的一方 | ⬜ |
| `MATCHING` | 领队退出 | 尚未确认且符合退出规则 | `OPEN` | 保留原状态并说明限制 | ⬜ |
| `CONFIRMED` | 开始活动 | 到达允许开始时间 | `IN_PROGRESS` | 保留原状态并记录失败 | ⬜ |
| `IN_PROGRESS` | 完成活动 | 活动已开始 | `DONE` | 保留原状态并允许重试 | ⬜ |
| `CONFIRMED` | 异常取消 | 二次确认并填写原因 | `CANCELLED` | 保留原状态并通知相关方 | ⬜ |

约束：所有状态转换由后端原子处理；每次转换返回最新项目数据；`DONE`/`CANCELLED` 为终态；取消须记录操作者、时间与原因并通知各方。

---

## 5. 非功能需求

### 5.1 性能
- 列表首屏可用时间尽量短；补充信息（发起人、路线）异步加载，不阻塞列表展示。
- 逐段步行规划应异步串行，避免阻塞 UI；结果可复用（详情页规划结果供全屏地图直接使用）。

### 5.2 安全
- 生产环境使用 HTTPS；Token 采用安全存储；不保存明文密码。
- 鉴权信息统一由拦截器注入，不在业务代码分散拼接。

### 5.3 稳定性与降级
- 高德地图 `MapView` 须正确管理生命周期（`onCreate/onResume/onPause/onDestroy` 等），避免切换/销毁时的原生崩溃（`GLMapEngine.nativeDestroy`）。
- 领队首屏为鉴权列表（`getProjects`），其 401/失败不得表现为"切换即闪退/被登出"；被动加载失败应给出空态或提示而非强制清栈跳转（结合 F-3.1-03）。
- 无高德 Key、无坐标、无网络时均应稳定降级并提示。

### 5.4 并发一致性
- 接单及状态转换的最终一致性由后端保证；客户端禁用按钮仅减少重复点击。

### 5.5 兼容性与适配
- 适配刘海屏/系统栏（`DisplayCutoutAdapter`）；固定尺寸控件需在主流机型不溢出、不裁切（如标签网格）。

### 5.6 可用性与资源
- 统一加载中/空数据/失败/重试样式；文案入资源文件，修复中文乱码；权限被拒后有清晰引导。

---

## 6. 外部接口依赖

领队端涉及的后端接口（`UserService`）：

| 接口 | 用途 | 相关功能 |
|---|---|---|
| `POST login` | 登录 | F-3.1-01 |
| `GET accounts/{id}` | 账号/发起人信息、地区 | F-3.3-01、F-3.4-01、F-3.9-01 |
| `GET accounts/{id}/leaderProfile` | 领队资料/资质 | F-3.9-03 |
| `POST accounts/{id}/username`、`.../intro` | 修改昵称/简介 | F-3.9-01 |
| `GET/POST accounts/{id}/tagPrefs` | 主题偏好 | F-3.9-02 |
| `uploadAvatar` | 头像上传 | F-3.9-01 |
| `GET projects` | 可接项目列表 | F-3.3-01 |
| `GET projects/{id}` | 项目最新详情 | F-3.4-01 |
| `GET projects/filter` | 条件筛选（可用于搜索） | F-3.3-05 |
| `GET routes/{id}` | 路线节点 | F-3.4-01、F-3.6 |
| `POST projects/{id}/leader` | 接单（分配领队） | F-3.5-01 |
| `GET regions/provinces`、`regions/{adcode}/children` | 省市联动 | F-3.3-03 |
| `POST chat/sessions` | 创建项目会话 | F-3.8-02 |
| 高德 `RouteSearch` / 地图 SDK | 步行规划与地图绘制 | F-3.6 |
| （建议新增）`confirm/start/complete/cancel/quit` | 状态流转 | F-3.7 |
| （建议新增）评价提交/查询、认证提交 | 评价与资质 | F-3.9-03、F-3.10-02 |

---

## 7. 附录

### 7.1 实现状态汇总
- ✅ 已实现：登录与角色识别、角色切换、登录态与失效处理、底部导航、可接列表与刷新、标签与高级筛选、当前城市、项目详情与接单按钮状态机、接单（客户端）、步行规划、全屏真实路线、分段指引、地图降级、资料编辑、历史路线。
- 🟡 部分实现：接单后端原子性/防重复、主题偏好、评价查看、会话与项目群（本地/演示，待服务端同步）。
- ⬜ 待实现：站内搜索、双方确认、开始/完成/异常取消/退出、资质认证、用户对领队评价。

### 7.2 后续建议顺序
1. 稳定性与安全：地图生命周期崩溃、首屏 401 体验、HTTPS/Token 安全。
2. 状态流转闭环：确认、开始、完成、取消/退出接口与页面。
3. 服务端消息与项目群同步。
4. 资质认证与评价体系。
5. 站内搜索与体验完善。
